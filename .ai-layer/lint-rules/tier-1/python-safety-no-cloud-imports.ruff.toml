[lint]
select = ["S", "TID251"]

[lint.flake8-tidy-imports.banned-api]
"openai".msg = "Cloud API is prohibited; use src/modules/ollama_client.py."
"anthropic".msg = "Cloud API is prohibited; use src/modules/ollama_client.py."
"langchain".msg = "LangChain is prohibited in this local-only governed pipeline."
"llamaindex".msg = "LlamaIndex is prohibited in this local-only governed pipeline."
"assemblyai".msg = "Cloud transcription is prohibited; use local WhisperX only."
"deepgram".msg = "Cloud transcription is prohibited; use local WhisperX only."
"boto3".msg = "Cloud storage is prohibited for participant data."
"google.cloud".msg = "Cloud storage is prohibited for participant data."
"cohere".msg = "Cloud API is prohibited; use src/modules/ollama_client.py."
"replicate".msg = "Cloud API is prohibited; use src/modules/ollama_client.py."
"huggingface_hub".msg = "Hosted model APIs are prohibited for participant data."

[lint.per-file-ignores]
"tests/**" = ["S101", "S603", "S607"]
"implementation docs/**" = ["S"]
"src/modules/ollama_client.py" = ["S310"]
"src/modules/audit_emitter.py" = ["S110"]
