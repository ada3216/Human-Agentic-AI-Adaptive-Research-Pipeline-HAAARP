# Partial enforcement: max-complexity = 8 keeps gate-check functions bounded
# and reviewer-inspectable, supporting ARCHITECTURE.md's hard-gate pattern.
#
# RUFF-LIMITATION: Ruff cannot verify that Pass 2 entry points call all required
# prerequisite checks (anchor hash, lens lock, DPIA). Requires integration tests.
#
# CI-ALTERNATIVE: MOCK_LLM=true pytest tests/ -k "pass2" -v

[lint.mccabe]
max-complexity = 8
