# Partial enforcement: max-doc-length = 120 flags docstrings/comments over limit.
# Code line length governed by root line-length = 120 in python.sh.
#
# RUFF-LIMITATION: Ruff cannot enforce max_file_lines = 300 or max_function_lines = 50.
#
# CI-ALTERNATIVE: python3 -c "
# import pathlib, sys
# violations = [(f, sum(1 for _ in f.open())) for f in pathlib.Path('src').rglob('*.py') if sum(1 for _ in f.open()) > 300]
# [print(f'VIOLATION: {f} has {n} lines (max 300)') for f,n in violations]
# sys.exit(1 if violations else 0)"

[lint.pycodestyle]
max-doc-length = 120
